events {}
http {
  server {
    listen ${NGINX_PORT};
    location / {
      root /usr/share/nginx/html;
      try_files $uri /index.html;
    }
    location /api/ {
      proxy_pass http://localhost:${PORT}/;
    }
    location /data/ {
      root /api;
    }
  }
}